query FIND_USER_BY_ID($findByIdId: ID!) {
  findById(id: $findByIdId) {
    __typename
    ... on ErrorResponse {
      code
      error
      success
    }
    ... on UserMutationResponse {
      code
      message
      success
      user {
        email
        id
      }
    }
  }
}
